#!/bin/bash

# Use ImageMagic for Screenshot.  Leave a delay before taking it so can setup shot
# $$ adds process id for unique name
import -window root -pause 3 /home/iain/Pictures/screenshot_$$.png

